# Production environment variables (committed — public, non-secret values only).
# Used automatically during `next build` on Vercel. Update if you move to a
# custom domain (e.g. https://lilyhomeinteriors.com).
NEXT_PUBLIC_SITE_URL=https://lily-homes.vercel.app
